import 'package:flutter/material.dart';

class EspressoContent extends StatelessWidget {
  const EspressoContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Espresso Content Coming Soon!",
        style: TextStyle(color: Colors.white, fontSize: 16),
      ),
    );
  }
}
