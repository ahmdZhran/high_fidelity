import 'package:flutter/material.dart';

class MacchiatoContent extends StatelessWidget {
  const MacchiatoContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Macchiato Content Coming Soon!",
        style: TextStyle(color: Colors.white, fontSize: 16),
      ),
    );
  }
}