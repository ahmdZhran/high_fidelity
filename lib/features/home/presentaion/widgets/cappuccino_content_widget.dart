import 'package:flutter/material.dart';

class CappuccinoContent extends StatelessWidget {
  const CappuccinoContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Cappuccino Content Coming Soon!",
        style: TextStyle(color: Colors.white, fontSize: 16),
      ),
    );
  }
}