import 'package:flutter/material.dart';

class LatteContent extends StatelessWidget {
  const LatteContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Latte Content Coming Soon!",
        style: TextStyle(color: Colors.white, fontSize: 16),
      ),
    );
  }
}
